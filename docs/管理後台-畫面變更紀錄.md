# 管理後台 畫面變更紀錄

> 這份文件記錄「管理後台畫面」的設計與修改。
> 產品/設計端(Jim)會在本地做原型給大家看,實際程式由工程團隊實作。
> **每次畫面有調整,就在下方「變更紀錄」加一筆(日期 + 畫面 + 改了什麼 + 為什麼)。**

---

## 目前畫面基準(截至 2026-09-20)

後台程式全部在 `src/admin/`,路由 `/admin/*`,與前台分離。設計目標:**白癡都會用 + 無障礙**。

| 畫面 | 路由 | 重點 |
| :--- | :--- | :--- |
| 登入 / 開新帳號 | `/admin/login` | 原型登入(任意帳密進),尚未接真帳號系統 |
| 總覽 | `/admin` | 數字卡(上架店家/遊程、活動優惠、表單題數)+ 快速入口 |
| 店家管理 | `/admin/stores` | Google Place 匯入 + 列表 + **編輯特色/分類/上下架** + 綁定活動優惠 + 搜尋 |
| 活動與優惠 | `/admin/offers` | 新增/刪除優惠或活動,綁定到店家 |
| 遊程編排 | `/admin/routes` | 挑店家、↑↓排序、上下架,存成精選遊程 |
| AI 表單設計 | `/admin/form` | 題目/選項增刪、↑↓排序、單選/複選/自由填答 |
| 評論與旅遊資訊 | `/admin/insights` | 切換器(店家評論 / 旅遊資訊),檢視 + 手動重抓爬蟲 |

**共用**
- 側邊選單(電腦)/ 漢堡抽屜(手機);每頁有標題 + 一句白話說明 + 明確主要按鈕 + 空狀態。
- 無障礙:44px 觸控目標、`<label>` 綁定、`role`/`aria`、Esc 關閉、操作後 toast 回饋。
- 假資料 + localStorage:`src/admin/data.ts`(種子取自前台 mock);原型登入:`src/admin/auth.ts`。
- 響應式:電腦、手機都需支援。

---

## 變更紀錄

### 2026-09-25 活動與優惠：時間區間排程介面

- 新增與編輯視窗可選擇設定開始上架、結束下架時間；結束時間必須晚於開始時間。
- 列表與檢視視窗顯示時間區間及依目前時間計算的狀態（待上架／已上架／已結束／已停用）。
- 此處僅完成前端欄位與狀態預覽；正式定時上下架與資料庫儲存由後端實作。

### 2026-09-25 金門商家：預設停留時間與照片

- 商家編輯表單新增預設停留時間（1–1440 分鐘）與照片管理（最多 5 張，JPG／PNG／WebP，單張 15MB 以內）。
- 商家列表顯示預設停留時間；有照片時以「照片 N 張」按鈕開啟檢視視窗。
- 既有瀏覽器資料讀取時會補上停留時間與空照片清單；照片目前儲存在此瀏覽器的原型資料中。

### 2026-09-20 建立基準
- 建立本文件,記錄管理後台目前七個畫面與其行為(如上基準表)。

### 2026-09-20 總覽:快速開始按鈕改標題
- 畫面:總覽(`/admin`)的「快速開始」五張捷徑卡。
- 改了什麼:把五個按鈕標題換成更貼近實際任務的說法。
  1. 匯入 / 管理店家 → 管理「金門縣」商家
  2. 維護活動與優惠 → 綁定商家活動與優惠
  3. 編排島轉遊程 → 編排精選遊程
  4. 設計 AI 表單 → 設計 AI 客製化問卷
  5. 檢視評論與資訊 → 檢視 Google 評論與網路情報
- 為什麼:標題直接講清楚每個入口在做什麼(地區範圍、資料來源、產出形式),讓不熟系統的人一眼看懂要點哪張。

### 2026-09-20 總覽:快速開始移除子標題
- 畫面:總覽(`/admin`)的「快速開始」五張捷徑卡。
- 改了什麼:拿掉每張卡第二行的白話說明,只留主標題。
- 為什麼:新標題已把任務講清楚,子標題變重複資訊,拿掉後版面更乾淨。

### 2026-09-20 總覽:統計卡拆細
- 畫面:總覽(`/admin`)上方四張數字卡。
- 改了什麼:
  1. 「上架店家、上架遊程」的「共 N 家 / 共 N 條」從數字旁移到卡片右下角。
  2. 「活動與優惠」拆成「活動 N 則」「優惠 N 則」兩個數字(依 `offers.kind` 區分)。
  3. 「表單題目」拆成「單選 / 複選 / 自由問答」各幾題(依 `form.type` 區分)。
- 為什麼:一眼看出結構比例(上架率、活動與優惠比重、題型組成),而不是只有一個總數。

### 2026-09-20 總覽:統計卡改名
- 畫面:總覽(`/admin`)數字卡。
- 改了什麼:「上架店家」→「上架商家」、「表單題目」→「問卷題目」。
- 為什麼:用詞和商家頁、AI 客製化問卷一致。

### 2026-09-20 側邊選單改名
- 畫面:後台側邊選單(電腦)/ 漢堡抽屜(手機)的六個項目。
- 改了什麼:
  1. 店家管理 → 金門商家
  2. 遊程編排 → 精選遊程
  3. AI 表單設計 → AI 客製化問卷
  4. 評論與旅遊資訊 → 評論與網路情報
  (總覽、活動與優惠 維持不變)
- 為什麼:選單名稱與各頁實際內容對齊,和快速開始的說法一致。

### 2026-09-20 商家頁:標題、匯入行為、編輯視窗
- 畫面:店家管理頁(`/admin/stores`)。
- 改了什麼:
  1. 大標「店家管理」→「管理商家」;小標改成操作指引:(1) 點匯入商家自動從 Google 匯入;(2) 剛匯入的商家未上架,請找到商家把上架開關打開;(3) 系統每個月自動匯入一次。
  2. 右上角按鈕「匯入店家」→「匯入商家」;點擊**不再跳視窗**,直接呼叫 Google Place API(原型以候選資料模擬)匯入,匯入過程按鈕 icon 轉圈,完成跳 toast「匯入完成」。新匯入的商家預設未上架。
  3. 編輯視窗:移除「刪除」按鈕;上架區塊文案改成「是否上架?」並移除下方小字說明(開關仍反映該店實際上架狀態)。
- 為什麼:標題直接給操作步驟、匯入一鍵完成不打斷、編輯視窗收斂到只做安全的修改(避免誤刪)。
- 備註:你說的「預設打開」我讓開關反映商家「實際」上架狀態(新匯入的是關的,符合小標(2)的流程);若要不管狀態一律預設開,再跟我說。

### 2026-09-20 商家頁:綁定數拆成活動 / 優惠
- 畫面:商家列表每張卡的座標下方。
- 改了什麼:原本「綁定 N 則活動優惠」合寫,改成像總覽一樣分開:「綁定 活動 N 則、優惠 N 則」(依 `offers.kind` 區分,某類為 0 就不列)。
- 為什麼:和總覽統計卡口徑一致,一眼看出綁的是活動還是優惠。

### 2026-09-20 用詞統一:店家 → 商家
- 畫面:後台所有頁面。
- 改了什麼:網頁上所有「店家」字樣改成「商家」(商家頁標題/搜尋/欄位、活動與優惠、精選遊程、AI 客製化問卷相關、評論頁「商家評論」、登入副標等);原本內文提到的舊選單名「店家管理」一併改成現用的「金門商家」。
- 為什麼:全站統一用「商家」。

### 2026-09-20 各頁大標題對齊快速開始按鈕
- 畫面:五個功能頁的大標題(總覽維持不變)。
- 改了什麼:標題與總覽「快速開始」按鈕文案一字不差對齊。
  - 商家頁:管理商家 → 管理「金門縣」商家
  - 活動優惠:活動與優惠 → 綁定商家活動與優惠
  - 遊程:遊程編排 → 編排精選遊程
  - 問卷:AI 表單設計 → 設計 AI 客製化問卷
  - 評論:評論與旅遊資訊 → 檢視 Google 評論與網路情報
- 為什麼:從總覽點按鈕進到頁面,標題和剛剛點的文字一致,不會有「點 A 進 B」的斷裂感。
- 備註:側邊選單為求簡短仍用短名(金門商家、活動與優惠、精選遊程、AI 客製化問卷、評論與網路情報),與頁面大標題是刻意的長短搭配。

### 2026-09-20 活動優惠頁:小標改寫
- 畫面:綁定商家活動與優惠頁的小標。
- 改了什麼:「新增優惠或活動,綁定到商家後,旅客在地圖 pin 上會看到對應標籤。」→「綁定到商家後,旅客可以在地圖上看到商家對應標籤」。
- 為什麼:拿掉技術字「pin」,用白話講清楚結果。

### 2026-09-20 活動優惠頁:新增視窗標題
- 畫面:活動優惠頁「新增」視窗標題。
- 改了什麼:「新增活動 / 優惠」→「新增活動或優惠」。
- 為什麼:與頁面用詞一致,去掉斜線。

### 2026-09-20 活動優惠頁:新增編輯功能
- 畫面:活動優惠列表每一列。
- 改了什麼:每列加「編輯」按鈕;點了開啟視窗(標題「編輯活動或優惠」、按鈕「儲存」),可修改綁定商家、類型、內容。新增與編輯共用同一個視窗。
- 為什麼:原本只能刪除重建,現在能直接改,少一步。

### 2026-09-20 活動優惠頁:上下架開關 + 刪除二次確認
- 畫面:活動優惠列表每一列。
- 改了什麼:
  1. 每列加「上架」開關與狀態標籤(已上架/已下架),可單獨控制每則活動或優惠是否露出;資料多了 `published` 欄位(預設上架)。
  2. 刪除改成先跳確認視窗(標題「確定要刪除?」,說明用口語:「刪掉『XXX』這則活動/優惠後就找不回來了,確定要刪嗎?」+ 取消/刪除),避免誤刪。
- 為什麼:上下架比刪除更常用也更安全;刪除是不可逆動作,需要再確認一次。

### 2026-09-20 遊程頁:增刪修對齊活動優惠頁
- 畫面:精選遊程列表與編輯視窗。
- 改了什麼:
  1. 列上直接放「編輯 + 刪除」兩顆鈕(原本只有編輯,刪除藏在編輯視窗內)。
  2. 刪除改成先跳確認視窗(「確定要刪除?」+ 口語說明「刪掉『XXX』這條遊程後就找不回來了,確定要刪嗎?」+ 取消/刪除)。
  3. 編輯視窗內移除刪除按鈕;新增時底部鈕顯示「新增」、編輯時顯示「儲存」。
  (上架開關與狀態標籤原本就有,維持。)
- 為什麼:三個管理頁(商家/活動優惠/遊程)的增刪修操作一致,學一次就會。

### 2026-09-20 遊程頁:小標改寫 + 拖曳排序 + 停留時間
- 畫面:精選遊程頁小標、編輯/新增視窗的路線站點。
- 改了什麼:
  1. 小標:「挑選商家、排好順序,存成一條島轉精選遊程。上架後會出現在旅客的遊程列表。」→「挑選商家並排好順序,提供島轉精選遊程給旅客」。
  2. 站點排序由「上下箭頭」改成「拖曳」:每站左側加把手,滑鼠與觸控都能拖(pointer 事件),拖動中該站高亮;標題旁加提示「拖曳左側把手可調整順序」。
  3. 每站新增「停留時間」欄位,單位分鐘(數字輸入 + 「分鐘」),存進遊程資料(`stays`,以 storeId 為 key)。
- 為什麼:拖曳比按箭頭直覺;停留時間讓遊程能標示每站建議停留多久。

### 2026-09-20 問卷頁:小標 + 增刪修上下架 + 拖曳排序
- 畫面:AI 客製化問卷頁。
- 改了什麼:
  1. 小標:「設定旅客在「AI 客製」要回答的題目。送出後會交給 AI 生成專屬路線。」→「設定旅客想要 AI 客製化遊程時,要回答的題目」。
  2. 每列對齊其他頁:加上架開關與狀態標籤(題目多了 `published` 欄位,未設定視為上架)、編輯鈕加鉛筆圖示、刪除獨立成一顆鈕並跳確認視窗(口語說明「刪掉『XXX』這個題目後就找不回來了,確定要刪嗎?」),編輯視窗內不再放刪除;新增顯示「新增」、編輯顯示「儲存」。
  3. 題目排序由「上下箭頭」改成「拖曳」(左側把手,滑鼠/觸控皆可,拖動中整列高亮),並加提示「拖曳左側把手可調整題目順序」。
- 為什麼:四個管理頁(商家/活動優惠/遊程/問卷)的增刪修與排序操作全部統一。

### 2026-09-20 評論情報頁:文案調整
- 畫面:檢視 Google 評論與網路情報頁。
- 改了什麼:
  1. 小標改成「系統每個禮拜自動抓取一次」。
  2. 切換器:「旅遊資訊」→「網路情報」(商家評論不變)。
  3. 網路情報那頁標題「在地旅遊資訊」→「網路情報總結」,重新抓取按鈕文字「重新抓取資訊」→「重新抓取情報」(與評論頁邏輯一致)。
- 為什麼:用詞統一為「網路情報」,並說明抓取頻率。
